{% extends "_layouts/base" %}
{% import '_macros/global' as global %}

{% block content %}

{# Header #}
    <section
        class="space-menu space-b">
        {# Placeholder Header Content #}
        {% set content = { 
            label: '404',
            title: 'This page couldn\'t be found',
            detail: 'Either this page doesn\'t exist anymore or there\'s a typo in the URL.',
            action: '/',
            actionText: 'Keep Browsing'
         } %}

        {# Page Header #}
        {% include "_components/static/page-header" with { content: content } %}
        </section>
        

    


{% endblock %}