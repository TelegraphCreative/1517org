{% extends "_layouts/_base" %}
{% import '_macros/global' as global %}

{% block content %}
    {# Header #}
    <section
        class="space-menu space-b">
        {# Placeholder Header Content #}
        {% set content = { title: 'Contact Us' } %}

        {# Page Header #}
        {% include "_components/static/page-header" with { content: content } %}</section>

    {# Content #}
    <div
        class="container--sm gutter-xl | space-b--lg" id="contact">

        {# Contact Form #}
        {% include '_components/form-speaker'  %}
    </div>


    {# Fullwidth Image #}
    {{ global.image( '', '-is16x9 -fit-cover bg-ivory-lightest', '/assets/placeholder/placeholder_11.jpg' )}}

{% endblock %}
