{% extends "_layouts/_base" %}
{% import '_macros/global' as global %}

{% block content %}
        
        {# Header #}
        <section
            class="space-menu space-b">
            {# Placeholder Header Content #}
            {% set content = { title: 'Request a Speaker' } %}

            {# Page Header #}
            {% include "_components/static/page-header" with { content: content } %}</section>

        {# Content #}
        <div
            class="container--xs gutter-xl | space-b--lg" id="contact">

            {# Contact Form #}
            {% include '_components/form-speaker'  %}
        </div>


        {# Fullwidth Image #}
        {{ global.image( '', '-is4x3 -fit-cover bg-ivory-lightest', '/assets/placeholder/placeholder_11.jpg' )}}
    

{% endblock %}
