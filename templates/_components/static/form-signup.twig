{% import '_macros/global' as global %}
{% if header %}
    <h2 class="text-white text-lg font-normal mb-2">{{ header }}</h2>
{% endif %}
<div class="signup-form">
    <form action="">
        <input type="text" class="email-input" placeholder="Email Digest Sign Up" />
        <button class="submit">
            {{ global.utilityIcon('paperplane', 'icon -xs -tilt-up') }}
        </button>
    </form>
</div>
