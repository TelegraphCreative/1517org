{% import '_macros/global' as global %}
{% if blocks|length %}

    {% for block in blocks %}
        {% switch block.type %}

        {# Article Body #}
        {% case "articleBody" %}
            {% include '_components/longform-blocks/article-body' %}

        {# Big List - Assets #}
        {% case "bigListAssets" %}
            {% include '_components/longform-blocks/big-list-assets' %}
        {% case "bigListAssetsLinks" %}
            {% include '_components/longform-blocks/big-list-assets' %}
       
        {# Button Group #}
        {% case "buttonGroup" %}
            {% include '_components/longform-blocks/button-group' %}
            
        {# Hero Image - full-bleed #}
        {% case "heroImageFullBleed" %}
            {% include '_components/longform-blocks/hero-image' %}
        {# NOTE: remain field when in BE -john #}
        {% case "imageFullBleed" %}
            {% include '_components/longform-blocks/hero-image' %}

        {# Entry Callout #}
        {% case "entryCallout" %}
            {% include '_components/longform-blocks/entry-callout' %}
            
        {# Billboard - floated image #}
        {% case "billboardFloatedImage" %}
            {% include '_components/longform-blocks/billboard--floated' %}
        
        {# Article Body #}
        {% case "leadIn" %}
            {% include '_components/longform-blocks/lead-in' %}
        
        {# Blockquote #}
        {% case "blockquote" %}
            {% include '_components/longform-blocks/blockquote' %}

        {# Article Image Blocks #}
        {% case "articleImageBlocks" %}
            {% include '_components/longform-blocks/article-image-blocks' %}

        {# Image Slider #}
        {% case "imageSlider" %}
            {% include '_components/longform-blocks/image-slider' %}
        {# NOTE: I named this block Carousel based on airtable spec. didn't see #}
        {#       it being called imageSlider anywhere else -john #}
        {% case "imageCarousel" %}
            {% include '_components/longform-blocks/image-slider' %}

        {# Bio Cards #}
        {% case "bioCards" %}
            {% include '_components/longform-blocks/bio-cards' %}

        {# Billboard - 2up #}
        {% case "billboard2Up" %}
            {% include '_components/longform-blocks/billboard--2up' %}

        {# Big List Syllabus #}
        {% case "bigListSyllabus" %}
            {% include '_components/longform-blocks/big-list-syllabus' %}

        {# Speakers #}
        {% case "speakers" %}
            {% include '_components/longform-blocks/speakers' %}

        {% endswitch %}
    {% endfor %}

{% endif %}